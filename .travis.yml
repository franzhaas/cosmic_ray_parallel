language: python

matrix:
  include:
  - python: 3.4
  - python: 3.5
  - python: 3.6

install:
- pip install pipenv
- pipenv install --dev
- pipenv install -e .

script:
- cd test && robot accept.robot
